<h1><%= @list.name() %></h1>

<% if @list.tasks().any?() %>
  <h3>Here are all the tasks in this list:</h3>

  <ul>
    <% @list.tasks().each() do |task| %>
      <li><%= task.description() %> </li>
    <% end %>
  </ul>

  <% else %>
    <p>There are no tasks on this list!</p>
  <% end %>

<div class="container">
  <a href="/"><button class="btn">Home</a>
</div>

<h3>Add a task to <%= @list.name() %> </h3>

<form action="/tasks" method="post">
  <input id="list_id" name="list_id" type="hidden" value="<%= @list.id() %>">
  <!-- added <input id="list_id" name="list_id" type="hidden" value="<%= @list.id() %>"> line. This will add a list_id with the value of @list.id() in the params hash that will be sent from the form to a post("/tasks") route. This is required to initialize a new Task object with the correct list_id. -->

  <label for="description">Description of the task:</label>
  <input id="description" name="description" type="text">

  <button type="submit">Add task</button>
</form>

<hr>

<div class="container">
  <a href="/">Home</a>
  <a href="/lists">View All Lists</a>
  <a href="/lists/new">Add New List</a>
  <a href="/lists/new">Add New Task</a>
  <a href="/lists/<%= @list.id() %>/edit">Edit <%= @list.name() %> list</a>
</div>
